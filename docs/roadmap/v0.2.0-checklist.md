# v0.2.0 Checklist

Status: execution checklist for the `v0.2.0` spaces layer.

## Space Scaffolding

- [ ] Create `.ftf/spaces/<name>/` layout
  - Notes: include `space.json`, `topics/`, `cas/`, `keys/`
  - Validation: `ftf space new lab` creates expected directory tree

- [ ] Implement `ftf space new`
  - Notes: initialize realm, space id, default topic, active member
  - Validation: `space.json` created and `ftf space show lab` reports expected fields

- [ ] Implement `ftf space show` / `ftf space ls`
  - Notes: no mutation, read-only inspection commands
  - Validation: lists existing spaces and reports counts without modifying files

## Mnemonic / Identity Derivation

- [ ] Add root identity generation for spaces
  - Notes: mnemonic optional but first-class
  - Validation: `ftf space new --mnemonic` emits mnemonic and stable pubkey/peer id

- [ ] Implement `ftf identity new`
  - Notes: standalone identity outside any space
  - Validation: emits mnemonic/pubkey/peer id and optional key output file

- [x] Implement `ftf identity derive`
  - Notes: supports `--path` and `--role`; writes deterministic member key files under space `keys/`
  - Validation: repeated derivation for same space/path is stable

- [x] Implement `ftf identity use` / `ftf identity show`
  - Notes: active signer resolution for command wrappers
  - Validation: switching active member updates `space.json` and is reflected in `show`

## Local Storage Layout

- [ ] Define `space.json` schema and load/save path
  - Notes: keep file minimal and deterministic
  - Validation: round-trip parse/write preserves expected fields

- [ ] Define `members.json` schema
  - Notes: include id, pubkey, role, local metadata
  - Validation: `ftf space member add` inserts deterministic entry

- [ ] Scope CAS and topic logs per space
  - Notes: avoid mutating frozen protocol semantics
  - Validation: `ftf put --space` writes inside selected space, not global default

## Command Wrappers

- [ ] Implement `ftf put --space`
  - Notes: path -> CAS -> signed `put` append
  - Validation: emits artifact/event hash and appends valid topic line

- [x] Implement `ftf xform --space`
  - Notes: resolve input/output path/hash, wrap existing core message creation
  - Validation: `ftf trace --space` shows emitted lineage from wrapper-generated event

- [ ] Implement `ftf attest --space`
  - Notes: resolve path/alias/hash target
  - Validation: trace shows `trace.note` with `note_type=\"attest\"`

- [ ] Implement `ftf revoke --space`
  - Notes: preserve non-destructive annotation semantics
  - Validation: trace shows revoke note and lineage remains present

- [x] Implement `ftf alias set/get/ls --space`
  - Notes: alias topic wrapper over `alias_claim`
  - Validation: `alias set/get/ls` round-trips deterministically against the space-local alias topic

## Invitation / Export Bundle Format

- [ ] Define public export bundle format
  - Notes: space metadata only
  - Validation: `space export --public` and `space import` round-trip

- [ ] Define invitation bundle format
  - Notes: include role grant and bootstrap metadata
  - Validation: `space invite` -> `space import` installs expected member/space state

- [ ] Implement `ftf space export/import`
  - Notes: support public, bundle, member-targeted exports
  - Validation: import/export round-trips without losing deterministic fields

## Bundle-First Sync

- [ ] Implement `ftf sync heads`
  - Notes: report topic heads only
  - Validation: stable output for unchanged space state

- [ ] Implement `ftf sync pull` / `ftf sync push`
  - Notes: file/bundle based first, no daemon requirement
  - Validation: exported bundle imported into second space reproduces heads and trace results

## Docs / Quickstart

- [ ] Add spaces quickstart
  - Notes: `space new -> put -> xform -> trace`
  - Validation: commands runnable as written on clean repo state

- [ ] Update README for spaces workflow
  - Notes: keep kernel vs workflow distinction explicit
  - Validation: README links to spaces docs and commands remain current

## Conformance Additions

- [ ] Extend harness coverage for wrapper-generated messages
  - Notes: wrappers must preserve same wire semantics as low-level commands
  - Validation: `bash verify-wire.sh` remains green

- [ ] Add proof receipt for first spaces workflow
  - Notes: bounded scenario only
  - Validation: receipt captures exact commands, hashes, and expected outputs

## Observatory Follow-Up

- [ ] Add support for hosted spaces sample bundle
  - Notes: optional UI follow-up, not a protocol blocker
  - Validation: Observatory loads sample spaces artifact/log set without custom backend

- [ ] Add source labeling for spaces bundle imports if exposed in UI
  - Notes: maintain provenance of loaded inspection content
  - Validation: UI distinguishes embedded/fetched/dropped sources
